services:
  quasar:
    build:
      context: .
      dockerfile: docker/Dockerfile.local
    ports:
      - "9000:9000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - HOST_UID=${UID:-1000}
    stdin_open: true
    tty: true
    networks:
      - thoughtless

networks:
  thoughtless:
    external: true
