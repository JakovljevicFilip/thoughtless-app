/**
 * CommandPayload
 * -----------------------------------------------------------------------------
 * Marker interface for data passed into a write-side CQRS command.
 * Concrete commands define their own payload shape.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface CommandPayload {}
