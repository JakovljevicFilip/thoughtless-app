/**
 * AggregateRepository
 * -----------------------------------------------------------------------------
 * Domain-level contract describing persistence operations for an aggregate.
 * Implementations belong in the Infrastructure layer.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface AggregateRepository {}
